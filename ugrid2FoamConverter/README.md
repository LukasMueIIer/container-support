# ugrid2FoamConverter
 A converter which reads in the ugrid mesh format and converts it into a polyMesh for openFOAM.

# Installation
execute pip install -e . in this directory. Examples in developmentFoamCases

# Requirements
- pyNastran 