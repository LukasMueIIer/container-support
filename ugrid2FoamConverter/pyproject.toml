[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mesh_converter"
version = "0.1.0"
description = "Internal tool to convert AFLR UGRID to OpenFOAM"
dependencies = [
    "numpy",
    "scipy",
    "pyNastran" 
]
# No [project.scripts] section, as requested.